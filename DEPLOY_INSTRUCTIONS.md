# Инструкция по деплою на сервер

## Важно! Изменения видны только @first1523

Новые приветственные сообщения отображаются **ТОЛЬКО** для пользователя:
- Username: @first1523
- ID: 5302383529

Остальные пользователи видят старый дизайн без изменений.

## Что было изменено

### Новые сообщения для тестового пользователя:

**Без подписки:**
```
Привееет! Каролина на связи ✨

Скажи «нет» блокировкам и «да» скорости!
VenturaVPN — топ-1 VPN в РФ, и я знаю, почему:
...
```

**С активной подпиской:**
```
Твоя подписка на VenturaVPN уже активна!
Вот что у тебя сейчас:

📆 Подписка: {days_left} дней
📱 Устройств: до {max_devices}
⏳ Действует до: {end_date}

Наслаждайся свободным интернетом...
```

## Деплой на сервер

```bash
# На сервере выполните:
cd /opt/ventura-vpn-robot
git pull origin main
pm2 restart ventura-bot
```

## Проверка работы

1. Откройте бота от имени @first1523
2. Отправьте `/start`
3. Если подписки нет - увидите промо-сообщение "Привееет! Каролина на связи ✨ ..."
4. Если подписка есть - увидите детали подписки с днями и датой окончания

Для остальных пользователей ничего не изменится - они увидят старый дизайн.

## Откат изменений (если нужно)

```bash
cd /opt/ventura-vpn-robot
git revert HEAD
pm2 restart ventura-bot
```
